.\" Automatically generated by Pandoc 3.6.4
.\"
.TH "grk_compress" "1" "" "Version 10.0" "convert to JPEG 2000 format"
.SH NAME
grk_compress \- compresses images to JPEG 2000 format
.SH SYNOPSIS
.PP
\f[B]grk_compress\f[R] [\f[B]\-i\f[R] infile.bmp] [\f[B]\-o\f[R]
outfile.j2k]
.SH DESCRIPTION
This program converts non\-\f[CR]JPEG 2000\f[R] images to the
\f[CR]JPEG 2000\f[R] format.
.IP \[bu] 2
Supported input formats: \f[CR]JPEG\f[R], \f[CR]BMP\f[R],
\f[CR]PNM\f[R], \f[CR]PGX\f[R], \f[CR]PNG\f[R], \f[CR]RAW\f[R],
\f[CR]RAWL\f[R] and \f[CR]TIFF\f[R]
.IP \[bu] 2
Supported input image extensions: \f[CR]jpg\f[R], \f[CR].jpeg\f[R],
\f[CR].bmp\f[R], \f[CR].pgm\f[R], \f[CR].pgx\f[R], \f[CR].pnm\f[R],
\f[CR].ppm\f[R], \f[CR].pam\f[R], \f[CR].png\f[R], \f[CR].raw\f[R],
\f[CR].rawl\f[R], \f[CR].tif\f[R] and \f[CR].tiff\f[R]
.IP \[bu] 2
Supported output formats: \f[CR]JP2\f[R], \f[CR]J2K\f[R]/\f[CR]J2C\f[R],
\f[CR]JPH\f[R], \f[CR]JHC\f[R] and \f[CR]MJ2\f[R] (Motion JPEG 2000)
.IP \[bu] 2
Supported output image extensions: \f[CR].jp2\f[R],
\f[CR].j2k\f[R]/\f[CR].j2c\f[R], \f[CR].jph\f[R], \f[CR].jhc\f[R] and
\f[CR].mj2\f[R]
.IP \[bu] 2
For \f[CR]PNG\f[R] the library must have \f[CR]libpng\f[R] available.
.IP \[bu] 2
For \f[CR]TIF/TIFF\f[R] the library must have \f[CR]libtiff\f[R]
available.
.IP \[bu] 2
For \f[CR]JPG/JPEG\f[R] the library must have a \f[CR]libjpeg\f[R]
variant available.
.PP
limitations
.IP \[bu] 2
\f[CR]grk_compress\f[R] supports up to and including 16 bit sample
precision for input images.
This is a subset of the ISO standard, which allows up to 38 bit
precision.
.PP
stdin
.PP
Input from \f[CR]stdin\f[R] is supported for the following formats:
\f[CR]PNG\f[R], \f[CR]JPG\f[R], \f[CR]RAW\f[R] and \f[CR]RAWL\f[R].
To read from \f[CR]stdin\f[R], make sure that the \f[CR]\-i\f[R]
parameter is \f[B]not\f[R] present, and that the \f[CR]\-\-in\-fmt\f[R]
parameter is set to one of the supported formats listed above.
.PP
Embedded ICC Profile (JP2Only)
.PP
If there is an embedded ICC profile in the input file, then the profile
will be stored in the compressed file.
.PP
IPTC (JP2 Only)
.PP
If an input \f[CR]TIF/TIFF\f[R] file contains \f[CR]IPTC\f[R] metadata,
this metadata will be stored in the compressed file.
.PP
XMP (JP2Only)
.PP
If an input \f[CR]TIF/TIFF\f[R] or \f[CR]PNG\f[R] file contains
\f[CR]XMP\f[R] metadata, this metadata will be stored in the compressed
file.
.PP
EXIF (JP2 only)
.PP
If an input \f[CR]JPEG\f[R] or \f[CR]PNG\f[R] file contains
\f[CR]EXIF\f[R] metadata, this metadata will be automatically stored in
the compressed file.
.PP
When only the input and output files are specified, the following
default option values are used:
.IP
.EX
* lossless compression
* reversible DWT 5\-3
* single quality layer
* single tile
* precinct size : 2\[ha]15 x 2\[ha]15 (i.e. only 1 precinct)
* code block dimensions : 64 x 64
* number of resolutions (i.e. DWT decomposition levels + 1) : 6
* no SOP markers
* no EPH markers
* default encode mode
* progression order : \[ga]LRCP\[ga]
* no ROI up\-shifted
* no image origin offset
* no tile origin offset
.EE
.PP
\f[B]Important note on command line argument notation below\f[R]: the
outer square braces appear for clarity only, and \f[B]should not\f[R] be
included in the actual command line argument.
Square braces appearing inside the outer braces \f[B]should\f[R] be
included.
.SS Options
\f[CR]\-h, \-help\f[R]
.PP
Print a help message and exit.
.PP
\f[CR]\-version\f[R]
.PP
Print library version and exit.
.PP
\f[CR]\-v, \-verbose\f[R]
.PP
Output information and warnings about encoding to console (errors are
always output).
Default is false i.e.\ console is silent by default.
.PP
\f[CR]\-i, \-\-in\-file [file]\f[R]
.PP
Input file.
Either this argument or the \f[CR]\-\-batch\-src\f[R] argument described
below is required.
See above for supported input formats.
.IP \[bu] 2
\f[CR]PNG\f[R] requires \f[CR]libpng\f[R] while \f[CR]TIF/TIFF\f[R]
requires \f[CR]libtiff\f[R]
.IP \[bu] 2
\f[CR]JPG\f[R] requires \f[CR]libjpeg\f[R] (or
\f[CR]libjpeg\-turbo\f[R]), and only 8 bit precision is supported
.IP \[bu] 2
For \f[CR]BMP\f[R] format, the coder accepts 24 bits color images and 8
bits (RLE or no\-RLE) images
.IP \[bu] 2
\f[CR]TIF\f[R] files can have up to 16 bits per component.
.IP \[bu] 2
For \f[CR]RAW\f[R] or \f[CR]RAWL\f[R] (\f[CR]RAW\f[R] \f[CR]L\f[R]ittle
endian) images, the \f[CR]\-F\f[R] parameter must be used (see below).
In the case of raw images with a component depth value between 9 and 16
bits, each component\[cq]s data must be stored on two bytes
(\f[CR]RAW\f[R] format assumes big endian\-ness, \f[CR]RAWL\f[R] assumes
little endian\-ness) When using this option, the output file must be
specified using \f[CR]\-o\f[R].
.PP
\f[CR]\-o, \-\-out\-file [file]\f[R]
.PP
Output file.
Required when using \f[CR]\-i\f[R] option.
Valid output image extensions are \f[CR]J2K\f[R], \f[CR]JP2\f[R],
\f[CR]J2C\f[R], \f[CR]JPH\f[R], \f[CR]JHC\f[R], \f[CR]MJ2\f[R] and
\f[CR]MJP2\f[R].
.PP
\f[CR]\-y, \-\-batch\-src [input directory]\f[R]
.PP
Path to the folder where the images to be compressed are stored.
Either this argument or the \f[CR]\-i\f[R] argument described above is
required.
When image files are in the same directory as the executable, this can
be indicated by a dot \f[CR].\f[R] argument.
When using this option, output format must be specified using
\f[CR]\-O\f[R].
.PP
\f[B]MJ2 multi\-frame mode:\f[R] When the output file (\f[CR]\-o\f[R])
has a \f[CR].mj2\f[R] extension, all images in the input directory are
compressed into a single MJ2 (Motion JPEG 2000) file.
Files are sorted alphabetically to ensure deterministic frame order.
Example: \f[CR]grk_compress \-y /path/to/frames/ \-o output.mj2\f[R]
.PP
\f[CR]\-a, \-\-out\-dir [output directory]\f[R]
.PP
Output directory where compressed files are stored.
Only relevant when the \f[CR]\-\-batch\-src\f[R] flag is set.
Default: same directory as specified by \f[CR]\-y\f[R].
.PP
\f[CR]\-O, \-\-out\-fmt [J2K|J2C|JP2|JPH|JHC]\f[R]
.PP
Output format used to compress the images read from the directory
specified with \f[CR]\-\-batch\-src\f[R].
Required when \f[CR]\-\-batch\-src\f[R] option is used.
Supported formats are \f[CR]J2K\f[R], \f[CR]J2C\f[R], \f[CR]JP2\f[R],
\f[CR]JPH\f[R], \f[CR]JHC\f[R] and \f[CR]MJ2\f[R].
.PP
\f[CR]\-K, \-\-in\-fmt [pbm|pgm|ppm|pnm|pam|pgx|png|bmp|tif|raw|rawl|jpg]\f[R]
.PP
Input format.
Will override file tag.
.PP
\f[CR]\-F, \-raw [width,height,number of components,bit depth,[s,u]\[at]<dx1>x<dy1>:...:<dxn>x<dyn>]\f[R]
.PP
Raw input image characteristics.
Required only if RAW or RAWL (RAW little endian) input file is provided.
Note: If sub\-sampling is omitted, \f[CR]1x1\f[R] is assumed for all
components.
.PP
Example of a raw \f[CR]512x512\f[R] unsigned image with \f[CR]4:2:0\f[R]
sub\-sampling
.IP
.EX
   \-F 512,512,3,8,u\[at]1x1:2x2:2x2
.EE
.PP
\f[CR]\-A, \-\-rate\-\-control\-algorithm [0|1]\f[R]
.PP
Select algorithm used for rate control.
* 0: Bisection search for optimal threshold using all code passes in
code blocks.
Slightly higher PSNR than algorithm 1.
* 1: Bisection search for optimal threshold using only feasible
truncation points, on convex hull (default).
Faster than algorithm 0.
.PP
\f[CR]\-r, \-\-compression\-ratios [<compression ratio>,<compression ratio>,...]\f[R]
.PP
Note: not supported for Part 15 (HTJ2K) compression
.PP
Compression ratio values (double precision, greater than or equal to
one).
Each value is a factor of compression, thus 20 means 20 times
compressed.
Each value represents a quality layer.
The order used to define the different levels of compression is
important and must be from left to right in descending order.
A final lossless quality layer (including all remaining code passes)
will be signified by the value 1.
Default: 1 single lossless quality layer.
.PP
\f[CR]\-q, \-quality [quality in dB,quality in dB,...]\f[R]
.PP
Note: not supported for Part 15 (HTJ2K) compression
.PP
Quality values (double precision, greater than or equal to zero).
Each value is a PSNR measure, given in dB, representing a quality layer.
The order used to define the different PSNR values is important and must
be from left to right in ascending order.
A value of 0 signifies a final lossless quality layer (including all
remaining code passes) Default: 1 single lossless quality layer.
.PP
\f[CR]\-n, \-numresolutions [number of resolutions]\f[R]
.PP
Number of resolutions.
It corresponds to the \f[CR]number of DWT decompositions +1\f[R].
Default: 6.
.PP
\f[CR]\-b, \-\-code\-\-block\-dims [code block width,code block height]\f[R]
.PP
Code\-block size.
The dimension must respect the constraint defined in the JPEG\-2000
standard (no dimension smaller than 4 or greater than 1024, no
code\-block with more than 4096 coefficients).
The maximum value authorized is 64x64.
Default: 64x64.
.PP
\f[CR]\-c, \-\-precinct\-dims [  [prec width,prec height],[prec width,prec height],... ]\f[R]
.PP
Precinct dimensions.
Dimensions specified must be powers of 2.
Multiple records may be specified, in which case the first record refers
to the highest resolution level and subsequent records refer to lower
resolution levels.
The last specified record\[cq]s dimensions are progressively
right\-shifted (halved in size) for each remaining lower resolution
level.
Default: \f[CR]2\[ha]15x2\[ha]15\f[R] at each resolution i.e.\ precincts
are not used.
Note: the inner square brackets must actually be present.
.PP
Example for image with 6 resolutions :
.PP
\f[CR]\-c [256,256],[256,256],[256,256],[256,256],[256,256],[256,256]\f[R]
.PP
\f[CR]\-t, \-\-tile\-dims [tile width,tile height]\f[R]
.PP
Tile size.
Default: the dimension of the whole image, thus only one tile.
.PP
\f[CR]\-L, \-PLT\f[R]
.PP
Use PLT markers.
Default: off
.PP
\f[CR]\-X, \-TLM\f[R]
.PP
Use TLM markers.
Default: off
.PP
\f[CR]\-I, \-irreversible\f[R]
.PP
Irreversible compression (ICT + DWT 9\-7).
This option enables the Irreversible Color Transformation (ICT) in place
of the Reversible Color Transformation (RCT) and the irreversible DWT
9\-7 in place of the 5\-3 filter.
Default: off.
.PP
\f[CR]\-p, \-\-progression\-order\f[R] [progression order]
.PP
Progression order.
The five progression orders are : \f[CR]LRCP\f[R], \f[CR]RLCP\f[R],
\f[CR]RPCL\f[R], \f[CR]PCRL\f[R] and \f[CR]CPRL\f[R].
Default: \f[CR]LRCP\f[R].
.PP
\f[CR]\-Z, \-rsiz [rsiz]\f[R]
.PP
Profile, main level, sub level and version.
Note: this flag will be ignored if cinema profile flags are used.
.PP
\f[CR]\-N, \-\-guard\-bits [number of guard bits]\f[R]
.PP
Number of guard bits to use in block coder.
Must be between 0 and 7.
.PP
\f[CR]\-w, \-cinema2K [24|48]\f[R]
.PP
2K digital cinema profile.
This option generates a codes stream compliant with the DCI
specifications for 2K resolution content.
The value given is the frame rate, which can be either 24 or 48 fps.
The main specifications of the JPEG 2000 Profile\-3 (2K Digital Cinema
Profile) are:
.IP \[bu] 2
Image size = 2048 x 1080 (at least one of the dimensions should match
2048 x 1080)
.IP \[bu] 2
Single tile
.IP \[bu] 2
Wavelet transform levels = Maximum of 5
.IP \[bu] 2
Wavelet filter = 9\-7 filter
.IP \[bu] 2
Codeblock size = 32 x 32
.IP \[bu] 2
Precinct size = 128 x 128 (Lowest frequency sub\-band), 256 x 256 (other
sub\-bands)
.IP \[bu] 2
Maximum Bit rate for entire frame = 1302083 bytes for 24 fps, 651041
bytes for 48fps
.IP \[bu] 2
Maximum Bit rate for each color component= 1041666 bytes for 24 fps,
520833 bytes for 48fps
.IP \[bu] 2
Tile parts = 3; Each tile part contains data necessary to decompress one
2K color component
.IP \[bu] 2
12 bits per component.
.PP
\f[CR]\-x, \-cinema4k\f[R]
.PP
4K digital cinema profile.
This option generates a code stream compliant with the DCI
specifications for 4K resolution content.
The value given is the frame rate, which can be either 24 or 48 fps.
The main specifications of the JPEG 2000 Profile\-4 (4K Digital Cinema
Profile) are:
.IP \[bu] 2
Image size = 4096 x 2160 (at least one of the dimensions must match 4096
x 2160)
.IP \[bu] 2
Single tile * Wavelet transform levels = Maximum of 6 and minimum of 1
.IP \[bu] 2
Wavelet filter = 9\-7 filter
.IP \[bu] 2
Codeblock size = 32 x 32
.IP \[bu] 2
Precinct size = 128 x 128 (Lowest frequency sub\-band), 256 x 256 (other
sub\-bands)
.IP \[bu] 2
Maximum Bit rate for entire frame = 1302083 bytes for 24 fps
.IP \[bu] 2
Maximum Bit rate for each color component= 1041666 bytes for 24 fps
.IP \[bu] 2
Tile parts = 6; Each of first 3 tile parts contains data necessary to
decompress one 2K color component, and each of last 3 tile parts
contains data necessary to decompress one 4K color component.
.IP \[bu] 2
12 bits per component
.PP
\f[CR]\-U, \-broadcast [PROFILE [,mainlevel=X][,framerate=FPS] ]\f[R]
.PP
Broadcast compliant code stream
.IP \[bu] 2
\f[CR]PROFILE\f[R] must be one of { \f[CR]SINGLE\f[R], \f[CR]MULTI\f[R],
\f[CR]MULTI_R\f[R]}
.IP \[bu] 2
X must be between 0 and 11
.IP \[bu] 2
frame rate may be specified to enhance checks and set maximum bit rate
when Y > 0.
If specified, it must be positive.
.PP
\f[CR]\-z, \-IMF [PROFILE [,mainlevel=X][,sublevel=Y][,framerate=FPS]] ]\f[R]
.PP
Interoperable Master Format (IMF) compliant codestream.
.IP \[bu] 2
\f[CR]PROFILE\f[R] must be one of { \f[CR]2K\f[R], \f[CR]4K\f[R],
\f[CR]8K\f[R], \f[CR]2K_R\f[R], \f[CR]4K_R\f[R], \f[CR]8K_R\f[R]}
.IP \[bu] 2
X must be between 0 and 11
.IP \[bu] 2
Y must be between 0 and 9
.IP \[bu] 2
frame rate may be specified to enhance checks and set maximum bit rate
when Y > 0.
If specified, it must be positive.
.PP
\f[CR]\-P, \-POC [T<tile number 0>=resolution number start>,component number start,layer number end,resolution number end,component number end,progression order/T<tile number 1>= ...]\f[R]
.PP
Progression order change.
This specifies a list of progression orders and their bounds if a
progression order change is desired.
Note: there must be at least two progression orders specified.
.PP
Example: \f[CR]\-POC T0=0,0,1,3,2,CPRL/T0=0,0,1,6,3,CPRL\f[R]
.PP
\f[CR]\-S, \-SOP\f[R]
.PP
SOP marker is added before each packet.
Default: no SOP.
.PP
\f[CR]\-E, \-EPH\f[R]
.PP
EPH marker is added after each packet header.
Default: no EPH.
.PP
\f[CR]\-M, \-mode [value]\f[R]
.PP
Non\-default encode modes.
There are 7 modes available.
The first six are:
.IP \[bu] 2
BYPASS(LAZY) [1]
.IP \[bu] 2
RESET [2]
.IP \[bu] 2
RESTART(TERMALL) [4]
.IP \[bu] 2
VSC [8]
.IP \[bu] 2
ERTERM(SEGTERM) [16]
.IP \[bu] 2
SEGMARK(SEGSYM) [32]
.IP \[bu] 2
HT [64]
.PP
and they can be combined together.
If more than one mode is used, the values between the brackets
\f[CR][]\f[R] must be added together.
Default: no mode.
.IP
.EX
Example : RESTART(4) + RESET(2) + SEGMARK(32) => \-M 38
.EE
.PP
Mode HT [64], for High Throughput encoding, \f[I]cannot\f[R] be combined
with any of the other flags.
.PP
\f[CR]\-u, \-\-tile\-parts [R|L|C]\f[R]
.PP
Divide packets of every tile into tile\-parts.
The division is made by grouping Resolutions (R), Layers (L) or
Components (C).
The type of division is specified by setting the single letter
\f[CR]R\f[R], \f[CR]L\f[R], or \f[CR]C\f[R] as the value for this flag.
.PP
\f[CR]\-R, \-ROI [c=component index,U=upshifting value]\f[R]
.PP
Quantization indices upshifted for a component.
.PP
Warning: This option does not implement the usual ROI (Region of
Interest).
It should be understood as a \[lq]Component of Interest\[rq].
It offers the possibility to upshift the value of a component during
quantization step.
The value after \f[CR]c=\f[R] is the component number
\f[CR][0, 1, 2, ...]\f[R] and the value after \f[CR]U=\f[R] is the value
of upshifting.
U must be in the range \f[CR][0, 37]\f[R].
.PP
\f[CR]\-d, \-\-image\-offset [x offset,y offset]\f[R]
.PP
Offset of the image origin.
The division in tile could be modified as the anchor point for tiling
will be different than the image origin.
Keep in mind that the offset of the image can not be higher than the
tile dimension if the tile option is used.
The two values are respectively for \f[CR]X\f[R] and \f[CR]Y\f[R] axis
offset.
Default: no offset.
.PP
\f[CR]\-T, \-\-tile\-offset [x offset,y offset]\f[R]
.PP
Offset of the tile origin.
The two values are respectively for X and Y axis offset.
The tile anchor point can not be inside the image area.
Default: no offset.
.PP
\f[CR]\-Y, \-MCT [0|1|2]\f[R]
.PP
Specify explicitly if a Multiple Component Transform has to be used.
.IP \[bu] 2
0: no MCT
.IP \[bu] 2
1: RGB\->YCC conversion
.IP \[bu] 2
2: custom MCT.
.PP
For custom MCT, \f[CR]\-m\f[R] option has to be used (see below).
By default, \f[CR]RGB\f[R]\->\f[CR]YCC\f[R] conversion is used if there
are three components or more, otherwise no conversion.
.PP
\f[CR]\-m, \-\-custom\-mct [file]\f[R]
.PP
Use custom array\-based MCT of 32 bit signed values, comma separated,
line\-by\-line no specific separators between lines, no space allowed
between values.
If this option is used, it automatically sets \f[CR][\-Y|\-mct]\f[R]
option equal to 2.
.PP
\f[CR]\-Q, \-\-capture\-res [capture resolution X,capture resolution Y]\f[R]
.PP
Capture resolution in pixels/metre, in double precision.
.IP \[bu] 2
If the input image has a resolution stored in its header, then this
resolution will be set as the capture resolution, by default.
.IP \[bu] 2
If the \f[CR]\-Q\f[R] command line parameter is set, then it will
override the resolution stored in the input image, if present
.IP \[bu] 2
The special values \f[CR][0,0]\f[R] for \f[CR]\-Q\f[R] will force the
encoder to \f[B]not\f[R] store capture resolution, even if present in
input image.
.PP
\f[CR]\-D, \-\-display\-res [display resolution X,display resolution Y]\f[R]
.PP
Display resolution in pixels/metre, in double precision.
The special values \f[CR][0,0]\f[R] for \f[CR]\-D\f[R] will force the
encoder to set the display resolution equal to the capture resolution.
.PP
\f[CR]\-C, \-comment [comment]\f[R]
.PP
Add \f[CR]<comment>\f[R] in comment marker segment(s).
Multiple comments (up to a total of 256) can be specified, separated by
the \f[CR]|\f[R] character.
For example:
\f[CR]\-C \[dq]This is my first comment|This is my second\f[R] will
store \f[CR]This is my first comment\f[R] in the first comment marker
segment, and \f[CR]This is my second\f[R] in a second comment marker.
.PP
\f[CR]\-W, \-logfile [output file name]\f[R]
.PP
Log to file.
File name will be set to \f[CR]output file name\f[R]
.PP
\f[CR]\-H, \-\-num\-threads [number of threads]\f[R]
.PP
Number of threads used for T1 compression.
Default is total number of logical cores.
.PP
\f[CR]\-J, \-\-server [server URL]\f[R]
.PP
Server URL.
.PP
\f[CR]\-j, \-\-license [license]\f[R]
.PP
License key.
.PP
\f[CR]\-k, \-\-kernel\-build [kernel build options]\f[R]
.PP
Kernel build options for GPU plugin.
.PP
\f[CR]\-l, \-\-duration [duration]\f[R]
.PP
Duration in seconds for a batch compress job.
\f[CR]grk_compress\f[R] will exit when duration has been reached.
.PP
\f[CR]\-e, \-\-repetitions [number of repetitions]\f[R]
.PP
Number of repetitions, for either a single image, or a folder of images.
Default value is \f[CR]1\f[R].
Unlimited repetitions are specified by a value of \f[CR]0\f[R].
.PP
\f[CR]\-g, \-\-plugin\-path [plugin path]\f[R]
.PP
Path to Grok plugin, which handles T1 compression.
Default search path for plugin is in same folder as
\f[CR]grk_compress\f[R] binary.
.PP
\f[CR]\-G, \-\-device\-id [device ID]\f[R]
.PP
For Grok plugin running on multi\-GPU system.
Specifies which single GPU accelerator to run codec on.
If the flag is set to \f[CR]\-1\f[R], all GPUs are used in round\-robin
scheduling.
If set to \f[CR]\-2\f[R], then plugin is disabled and compression is
done on the CPU.
Default: \f[CR]0\f[R].
.SH FILES
.SH ENVIRONMENT
.SH BUGS
See GitHub Issues: https://github.com/GrokImageCompression/grok/issues
.SH AUTHOR
Grok Image Compression Inc.
.SH SEE ALSO
\f[B]grk_decompress(1)\f[R]
