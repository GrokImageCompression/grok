#define GRK_PACKAGE_VERSION "@PACKAGE_VERSION@"

#cmakedefine _LARGEFILE_SOURCE
#cmakedefine _LARGE_FILES
#cmakedefine _FILE_OFFSET_BITS @_FILE_OFFSET_BITS@
#cmakedefine GROK_HAVE_FSEEKO @GROK_HAVE_FSEEKO@

/* Byte order.  */
/* All compilers that support Mac OS X define either __BIG_ENDIAN__ or
__LITTLE_ENDIAN__ to match the endianness of the architecture being
compiled for. This is not necessarily the same as the architecture of the
machine doing the building. In order to support Universal Binaries on
Mac OS X, we prefer those defines to decide the endianness.
On other platforms we use the result of the TRY_RUN. */
#if !defined(__APPLE__)
#cmakedefine GROK_BIG_ENDIAN
#elif defined(__BIG_ENDIAN__)
# define GROK_BIG_ENDIAN
#endif

#cmakedefine GRK_BUILD_DCI @GRK_BUILD_DCI@
#cmakedefine GRK_BUILD_LIBTIFF @GRK_BUILD_LIBTIFF@
#define GROK_PLUGIN_NAME "@GROK_PLUGIN_NAME@"
